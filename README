sircd -- simple irc daemon
==========================

- Pure POSIX c99
- Adheres to RFC 1459
- Configuration is done by editing config.h and recompiling
- Logs to stdout
- UNIX sockets only

Licensed under GPL v3+

Caveats:
You need to use a forwarding process to access the interwebs; this allows you to fine tune connection and Security Through Obscurity
Currently only supports server <-> client
